{
  "name": "ai-workspace",
  "version": "1.0.0",
  "description": "Root workspace for MERN Multi-Tool Web Application",
  "scripts": {
    "install:all": "cmd.exe /c \"npm install --prefix server && npm install --prefix client\"",
    "start:server": "npm run dev --prefix server",
    "start:client": "npm run dev --prefix client",
    "dev": "npx concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
